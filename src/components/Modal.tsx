import styles from './modal.module.css';

const Modal = () => {
  return (
    <div className={'overlay'}>
      <div className={'modal'}>ESTE ES UN MODAL RE FACHERO</div>;
    </div>
  );
};

export default Modal;
